# Boston Housing — Price Prediction (Freelance-ready demo)

**What:** Predict house prices using RandomForest with feature engineering and saved model.

**Files included**
- `boston.csv` — dataset.
- `data_preprocess.py`, `feature_engineering.py`, `train_model.py`, `predict.py`, `visualize.py` — scripts.
- `boston_model.pkl`, `scaler.pkl` — saved model & scaler (after you run training).

## Installation
1. Clone repo:
